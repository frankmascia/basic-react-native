import { StyleSheet } from 'react-native';

//Define style for the main screen container
const screenStyles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 20,
    },
});

export default screenStyles;
