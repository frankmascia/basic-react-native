import { StyleSheet } from 'react-native';

//Define styles for the form component

const formStyles  = StyleSheet.create({
    sectionHeader: {
        fontSize: 18,
        fontWeight: 'bold',
        marginTop: 20,
    },
    input: {
        height: 40,
        borderColor: '#ccc',
        borderWidth: 1,
        paddingHorizontal: 10,
        marginTop: 10,
        borderRadius: 5,
    },
});

export default formStyles;
