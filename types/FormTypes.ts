//This file defines the state for the form that holds all user inputs
export interface FormState {
    firstName: string;
    lastName: string;
    phoneNumber: string;
    email: string;
    vehicleMake: string;
    vehicleModel: string;
    vehicleYear: string;
    timeSlot: string;
}
